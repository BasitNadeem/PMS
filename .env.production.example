# Production build-time variables
# Copy this to .env.production (gitignored) and fill in
# real values before running pnpm build:prod
#
# These are baked into the frontend bundles at BUILD TIME
# by Vite — they cannot be changed at runtime after building.
# If you change these, you must rebuild.

VITE_API_URL=https://api.innflo.co
